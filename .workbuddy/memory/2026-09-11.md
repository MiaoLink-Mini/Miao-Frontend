# 2026-09-11

- 将启动页 `pages/login/index` 的中央 logo 由图片 `<image src="/assets/golink.png">` 改回与参考项目 GoLink-Frontend 一致的纯 CSS 星号组件 `<activity-mark active size="large"/>`（桃橙色 8 棒米字 + 中心圆点，5s 旋转动画）。wxss 中 `.welcome-logo` 规则替换为 `.welcome-art activity-mark{transform:scale(1.25)}`，并移除矮屏媒体查询里的 `.welcome-logo` 规则。`activity-mark` 组件已在 app.json 全局注册，无需额外引入。
